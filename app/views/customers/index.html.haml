%h1 Customers#index
%p Find me in app/views/customers/index.html.haml
= form_tag "show_orders", method: :get do 
  = collection_select(:customer, :id, @customers.all, :id, :name_with_id)
  = submit_tag "Get this customer's orders"
 
    

 
 
