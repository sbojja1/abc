%h1 Customers#show
%p Find me in app/views/customers/show.html.haml
= "There are #{@orders.count} orders for customer #{@customer_id}"
= form_tag "show", method: :get do
  = collection_select(:order, :id, @orders.all, :id, :order_date)
  = submit_tag "Get this order's products"